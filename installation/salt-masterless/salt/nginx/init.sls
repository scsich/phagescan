# The nginx package is installed by the installation/scanmaster/PACKAGES.ubuntu.
# TODO: Create salt states that do the following:
#   - Install the nginx config file.
#   - Ensure nginx starts on boot.

