# TODO: Create salt states to build a production scan master.
#  Most of the states required are already built.
#  Use scan-master-worker-dev/init.sls as a guide.
#  - need to add celery.master, nginx, gunicorn, and supervisord
#  - need to remove dev stuff.
